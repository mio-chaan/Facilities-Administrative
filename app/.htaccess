# Nothing in this folder is ever meant to be served over HTTP.
# Belt-and-suspenders alongside the root .htaccess RewriteRule.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
